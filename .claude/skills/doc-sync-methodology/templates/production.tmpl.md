<!-- owner: doc-sync | to change: invoke tech-lead, then pm, then doc-sync — never edit this file directly -->

**Last Synced from PRD Revision**: <fill in PRD revision number> | **Last Updated**: <date>

---

## Project Summary

<!-- From PRD Project Overview. Copy the full text verbatim. Do not paraphrase, summarize, or add context not present in the PRD. -->

## Tech Stack

<!-- Exact match from PRD Tech Stack. Every entry must include name + version. Do not add entries not listed in PRD. If the PRD does not specify a version, write [AMBIGUITY: PRD does not specify version for <component>] in the Notes column. -->

| Component | Name + Version | Notes |
| --- | --- | --- |
<!-- Add one row per tech stack entry from PRD. Example row:
| Database | PostgreSQL 15.3 | |
| Backend runtime | Node.js 20.11 LTS | |
Do not add rows for technologies not mentioned in the PRD. -->

## Architecture

<!-- From PRD Architecture Overview. Copy the full text verbatim. If the PRD includes diagrams or ASCII art, reproduce them here. Do not add architectural decisions not stated in the PRD. -->

## Shared Conventions

<!-- Populated by Doc-Sync during initial sync from the ## Build Config section of status.md
     (which PM fills in during init by asking the user for the project's commands).
     Doc-Sync copies the Build, Lint, and Test values here exactly. Keys with blank values
     are omitted. self-check.sh and run-qa.sh read these exact key names to run automated checks.
-->

Build:
Lint:
Test:

## Module Index

<!-- Filled by Doc-Sync from PRD Module Breakdown. One row per module. Description must be the one-line summary from the PRD — do not write your own summary. If the PRD does not provide a one-line summary, copy the first sentence of the module description. -->

| MOD-ID | Module Name | Directory | Description |
| --- | --- | --- | --- |
<!-- One row per module. Directory = the mod-<name> folder name from the Module Map in status.md. Example:
| MOD-001 | Authentication Service | mod-auth | Handles user login, registration, and session management. |
| MOD-002 | Payment Gateway | mod-payments | Processes payments and manages transaction records. |
-->
